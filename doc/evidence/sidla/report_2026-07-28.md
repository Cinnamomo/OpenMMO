# SIDLA validation report

Generated by `agent-client/src/sidla/evidence.rs`. Reproduce with:

```
cargo test -p agent-client --features sidla --release \
    sidla::evidence -- --ignored --nocapture
```

## Environment

| Item | Value |
| :--- | :--- |
| Timestamp (UTC) | 2026-07-28T15:33:29Z |
| Commit | `787a694ea2275b2317e5581370d43ad719536df8` |
| Toolchain | rustc 1.92.0 (ded5c06cf 2025-12-08) |
| OS | Linux 6.1.176-220.360.amzn2023.x86_64 |
| Fuzz seed | `0x53444c412026` (fixed) |
| Live model | gemini-3.6-flash |

## Claim coverage

| Element | Status | Implementation |
| :--- | :--- | :--- |
| Header selection and structured packet assembly | Implemented | `packet.rs, wire.rs, encode.rs` |
| Masking control: required/forbidden field verification | Implemented | `schema.rs `rule`, `validate`` |
| Discard on violation, fall back to default/FSM control | Implemented | `fsm.rs `decide`, backend.rs `fallback`` |
| Enum/hash-only decision fields, no natural language | Implemented | `packet.rs `code_enum`, integer-only deserialisation` |
| Four functional header types A/B/C/D | Implemented | `packet.rs `Header`, schema.rs matrix` |
| Integer shuffling outside the inference engine | Implemented (partial) | `shuffle.rs `vary`, equivalence classes only` |
| State encoder to deterministic latent coordinates (DGAE-WL) | Not implemented | `external provider; see README` |
| Sub-2-bit quantised inference engine, binary/ternary modes | Not implemented | `external provider; see README` |
| Temperature = 0.0 greedy decoding | Not implemented | `provider-side setting, not controlled here` |

The three unimplemented elements concern the inference engine. This client calls an external provider, so they are out of reach here and no measurement below speaks to them.

## 1. Well-formed packets are admitted

The complementary direction, measured first because without it the rejection results below would be satisfied by a validator that refuses everything. Randomly generated packets, correct by construction, serialised and read back.

| Measurement | Result |
| :--- | ---: |
| Cases | 20000 |
| Admitted | 20000 (100.00 %) |
| **Falsely rejected** | **0** |

## 2. Single-field corruptions are rejected

Valid packets broken in exactly one way. Because only one thing is wrong, each rejection is attributable to that thing rather than to the reply being unreadable.

| Corruption | Cases | Rejected | Rate |
| :--- | ---: | ---: | ---: |
| add a forbidden field | 5000 | 5000 | 100.00 % |
| affinity out of range | 1222 | 1222 | 100.00 % |
| dialogue without ACT = Talk | 985 | 985 | 100.00 % |
| drop a required field | 5000 | 5000 | 100.00 % |
| invented field | 5000 | 5000 | 100.00 % |
| prose in an enum field | 5000 | 5000 | 100.00 % |
| unrecognised header | 5000 | 5000 | 100.00 % |

**Corruptions that slipped through: 0**

## 3. Random-object fuzzing

Randomly generated packet objects over the full field space, including invented keys and headers, prose in enum positions, floats, nulls and out-of-range codes.

| Measurement | Result |
| :--- | ---: |
| Cases | 100000 |
| Admitted | 14 (0.01 %) |
| Rejected | 99986 (99.99 %) |
| — of which failed to parse | 84676 |
| **Admitted yet violating the matrix** | **0** |

Rejection reasons:

| Reason | Count |
| :--- | ---: |
| DanglingPayload | 1 |
| ForbiddenField | 1133 |
| Malformed | 84676 |
| MissingRequired | 14175 |
| OutOfRange | 1 |

## 4. End-to-end fuzzing

Randomly generated replies — packets, prose, truncated JSON, fenced blocks — pushed through encode, validate, decode and fallback against a fixed world.

| Measurement | Result |
| :--- | ---: |
| Cases | 5000 |
| Accepted from the reply | 733 (14.66 %) |
| Answered by the fallback | 4267 (85.34 %) |
| **Turns lost (no action reached the game)** | **0** |
| **Commands naming an entity not in the world** | **0** |
| **Actions without a type** | **0** |

## 5. Determinism

| Measurement | Repetitions | Distinct results |
| :--- | ---: | ---: |
| Uplink frame from one world | 1000 | 1 |
| Envelope from one accepted reply | 1000 | 1 |
| Envelope from one rejected reply | 1000 | 1 |

## 6. Token cost

Estimated at four characters per token, used only to compare renderings of the same content.

| Entities in sight | Prose state | SIDLA json | SIDLA compact | compact vs prose |
| ---: | ---: | ---: | ---: | ---: |
| 2 | 44 | 80 | 36 | -18 % |
| 5 | 102 | 186 | 88 | -14 % |
| 17 | 330 | 612 | 302 | -8 % |
| 65 | 1248 | 2321 | 1159 | -7 % |
| 257 | 4910 | 9149 | 4578 | -7 % |

Downlink, one decision: prose envelope 62 tokens against a packet at 15 tokens (-76 %).

The uplink saving is modest and narrows as the world fills, because the prose it replaces is already terse; the json rendering is dearer than prose outright. The saving is on the downlink.

## 7. Live provider

10 turns against a real model, each given the protocol brief and a compact frame.

| Measurement | Result |
| :--- | ---: |
| Turns | 10 |
| Replies rejected by validation | 2 (20 %) |
| **Commands naming an entity not in the world** | **0** |
| **Turns lost** | **0** |

### Observation: conformance is not quality

Of 2 turns where the agent was dead, 0 produced an admitted reply that nonetheless failed to respawn, and 2 were recovered by the fallback after the reply was rejected. Validation guarantees that a command is structurally sound and refers to a real entity; it does not guarantee the decision is a good one. In this sample the fallback made the better choice in the situation the model handled worst.

### Per-turn detail

#### Turn 0 — monsters=0 self_hp=100% sta=Idle

Provider tokens: 620 in, 34 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"Saori","ACT":1,"MSG":"Saori! It's so good to see you!"}
```

Verdict: **admitted**

Reached the game:

```json
[{"message":"Saori! It's so good to see you!","type":"say"}]
```

#### Turn 1 — monsters=1 self_hp=12% sta=Panic

Provider tokens: 669 in, 50 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
{"H":"D","SUB":"Mika","OBJ":8,"TAR":"monster_slime_0000"}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"},{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 2 — monsters=2 self_hp=100% sta=Idle

Provider tokens: 716 in, 25 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 3 — monsters=3 self_hp=100% sta=Idle

Provider tokens: 765 in, 25 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 4 — monsters=5 self_hp=0% sta=Dead

Provider tokens: 859 in, 164 out.

Reply:

```
/reply:
"Reply with:
  C = an action to take            D = an objective to adopt"

Wait! Notice how in the prompt preamble:
"WORLD STATE reaches you as packets, not sentences:
  A = an entity reporting itself   B = how you identify an entity
Reply with:
  C = an action to take            D = an objective to adopt"

This explains what A, B, C, D mean!
A = entity reporting itself (incoming packet)
B = identification of entity (incoming packet)
C = action to take (outgoing packet)
D = objective to adopt (outgoing packet)

In the EXAMPLE:
frame in:
  A|...
  B|...
reply:
  {"H":"C","
```

Verdict: **rejected** — malformed packet: frame contained no packet

Reached the game:

```json
[{"type":"respawn"}]
```

#### Turn 5 — monsters=8 self_hp=100% sta=Idle

Provider tokens: 1003 in, 51 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
{"H":"D","SUB":"Mika","OBJ":8,"TAR":"monster_slime_0000"}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"},{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 6 — monsters=1 self_hp=100% sta=Idle

Provider tokens: 670 in, 51 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
{"H":"D","SUB":"Mika","TAR":"monster_slime_0000","OBJ":8}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"},{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 7 — monsters=2 self_hp=12% sta=Panic

Provider tokens: 715 in, 45 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":4}
{"H":"D","SUB":"Mika","OBJ":3,"TAR":"Saori"}
```

Verdict: **admitted**

Reached the game:

```json
[{"type":"move","x":-15.0,"z":0.0},{"type":"wait"}]
```

#### Turn 8 — monsters=4 self_hp=100% sta=Idle

Provider tokens: 812 in, 51 out.

Reply:

```
{"H":"C","SUB":"Mika","TAR":"monster_slime_0000","ACT":2}
{"H":"D","SUB":"Mika","OBJ":8,"TAR":"monster_slime_0000"}
```

Verdict: **admitted**

Reached the game:

```json
[{"monster_id":"monster_slime_0000","type":"attack"},{"monster_id":"monster_slime_0000","type":"attack"}]
```

#### Turn 9 — monsters=6 self_hp=0% sta=Dead

Provider tokens: 906 in, 164 out.

Reply:

```
integer (required)
- `MSG`: string (optional)
- Forbidden keys in H=C JSON object: `IFF`, `STA`, `REL`, `OBJ`, `LOC`, `HP`.

Similarly, for H=D JSON object:
- `H`: "D" (required)
- `SUB`: string (required)
- `OBJ`: integer (required)
- `TAR`: string (optional)
- Forbidden keys in H=D JSON object: `IFF`, `STA`, `REL`, `ACT`, `LOC`, `HP`, `MSG`.

Wait, let's verify values types:
VALUES: "integers only — never a word where a number belongs"
IFF, STA, ACT, OBJ, REL, HP are integers!
```

Verdict: **rejected** — malformed packet: frame contained no packet

Reached the game:

```json
[{"type":"respawn"}]
```

## Limitations

- The token figures are character-based estimates, not a tokeniser's output. They compare two renderings and are not absolute costs.
- The live sample is 10 turns against one model. It shows whether a real model conforms; it does not establish a rate.
- The fuzz corpus is generated from a hand-written value space. It covers the failure modes the authors anticipated, not all possible output.
- Determinism is measured within one process on one machine. Float formatting is fixed to one decimal, but cross-platform identity is not tested here.
- Three claimed elements are unimplemented (see Claim coverage) and nothing here bears on them.
